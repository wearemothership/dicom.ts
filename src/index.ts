import Renderer, { render } from "./renderer/Renderer";
import {
	Parser,
	parseImage,
} from "./parser";

export default {
	parseImage,
	render,
	Renderer,
	Parser,
	// parserError: Series.parserError,
};
